using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pingas : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //PINGAS!
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
